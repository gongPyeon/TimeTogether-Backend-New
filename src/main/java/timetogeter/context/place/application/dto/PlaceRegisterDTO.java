package timetogeter.context.place.application.dto;

public record PlaceRegisterDTO (
                        int placeId,
                        String placeName,
                        String placeUrl){
}
