package timetogeter.context.place.application.dto.response;

public record PlaceRegisterResDTO (int placeId,
                                   String placeName,
                                   String placeAddress,
                                   String placeInfo){
}
