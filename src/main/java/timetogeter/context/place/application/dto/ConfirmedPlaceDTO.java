package timetogeter.context.place.application.dto;

public record ConfirmedPlaceDTO(int placeId,
                                String placeName,
                                String placeAddress) {
}
